<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eric.seckill.dao.SkProjectMapper">
    <select id="listAllGoodsId" resultType="java.lang.String">
        SELECT
            goods_id
        FROM
            sk_project
        WHERE
            is_active = '1'
    </select>
</mapper>